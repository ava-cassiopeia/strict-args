{
  "name": "strict-args",
  "version": "0.2.0",
  "description": "A NodeJS arguments library which strictly enforces flag presence, command syntax, alerts on unexpected flags, and manages help and command outputs.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "ts-node node_modules/jasmine/bin/jasmine",
    "prepublishOnly": "npm run build",
    "manualtest": "ts-node manualtest/fake_cli"
  },
  "author": "Ava Mattie",
  "license": "ISC",
  "devDependencies": {
    "@types/jasmine": "^4.3.1",
    "jasmine": "^4.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "cli-table3": "^0.6.3"
  }
}
